# Backend (raíz del repo). En Railway: Root Directory vacío o "."
# Creá otro servicio con Root Directory "frontend" y su railway.toml.

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120

