# Solo BACKEND (Root Directory vacío).
# El servicio frontend debe usar frontend/railway.toml (no este archivo).

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120

