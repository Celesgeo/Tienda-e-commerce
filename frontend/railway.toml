# Solo para el servicio Railway con Root Directory = "frontend"
# En ese servicio: Settings → Config-as-code → File Path = frontend/railway.toml

[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 120
