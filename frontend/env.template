# Copiá a frontend/.env (no se sube a Git).
# cp env.template .env

# Desarrollo: proxy de Vite hacia el backend
# VITE_PROXY_TARGET=http://127.0.0.1:5000

# Producción (Railway): URL pública del API
# VITE_API_URL=https://tu-backend.up.railway.app/api
